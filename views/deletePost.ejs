<%- include('./layouts/header.ejs') %>

<div class="w-[100%] flex justify-center items-center h-[100vh]">
    <div class="w-auto h-auto border-b-2 border-black p-6 flex flex-col gap-6">
      <h1 class="font-bold text-3xl"> 
        It Will be Delete Permanently
      </h1>
      <div class="flex justify-between items-center w-[100%] ">
        <a href="/dashboard" class="px-4 py-2 font-bold text-green-300">Cancel</a>
        <a href="/deletePost" class="px-4 py-2 font-bold text-red-500">Delete</a>
      </div>
      <!-- <% if(typeof message !== 'undefined') {%>
              <p class="text-green-600"><%= message %></p>
              <% } %> -->
    </div>
</div>

<%- include('./layouts/footer.ejs') %>
